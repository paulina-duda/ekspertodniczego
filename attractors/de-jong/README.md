# De Jong Attractor

The De Jong attractor is rendered as a fine, layered point cloud. The classic
parameters are `a=-2.0`, `b=-2.0`, `c=-1.2`, `d=2.0`.

Render all colour variants:

```bash
python3 attractors/de-jong/source/de_jong_instagram.py --all-palettes
```
